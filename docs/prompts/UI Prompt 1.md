

Can we review the code generated by GPT-4.1 and re-write if it is not up the requirements of Enterprise Grade Platform
----------------

-------------------


The Granular Mega Prompt for Copilot
Copy and paste this into Copilot:
System Role: Act as a Lead UI/UX Engineer and Prisma Expert.
Objective: Generate a modular React/Next.js frontend architecture for a multi-tenant School Management System using the provided Prisma schema.
1. Data Table Schema Mapping (Micro-Level): For every page (Teachers, Students, Parents), implement a DataTable component that maps to the following logic: * Headers & Columns: > * Identification: Display id (truncated) or tenantCode where applicable. * Configuration Data: Parse the Json payload from ConfigMaster to display dynamic columns like "Department" for Teachers or "Grade" for Students. * System Metadata: Include "Last Updated" using the updatedAt field and "Status" derived from ModuleEntitlement.enabled. * Row Actions: > * Edit: Trigger a slide-over panel to patch the payload in Configuration or ConfigMaster. * Deactivate: A toggle switch that updates the Boolean status in ModuleEntitlement.
+2
2. Specific Page Definitions:
•	Teacher Management: Include columns for [Name, Subject (from payload), Joining Date, Status]. Add a "Bulk Upload" button next to "Create Teacher."
•	Student Directory: Include columns for [Admission No, Class, Section, Parent Contact (from payload), Enrollment Date]. * Entitlement Dashboard: A specialized view of the ModuleEntitlement table allowing the Admin to toggle "Exams," "Analytics," and "Events" modules on/off for the school.
3. UI Components & Layout:
•	Global Navigation: A collapsible sidebar. Navigation items must be filtered: navItems.filter(item => entitlementMap[item.key].enabled). * Search & Filter Bar: > * A search input that queries the configKey in ConfigMaster. * Dropdown filters for "Class" and "Section" that fetch unique values from the ConfigMaster payload. * Audit Component: A small "Activity Feed" in the sidebar footer displaying the last 5 ProcessedEvent entries (e.g., "Student Record Updated").
+1
4. Implementation Detail: Use Tailwind CSS for an "Enterprise Slate" theme. Use TanStack Table for the logic (sorting, filtering, pagination) and Zustand for state management of the tenantId.
Deliverable: Provide the code for the SchoolAdminDashboard layout and the UniversalDataTable component that accepts a configType prop to switch between Student, Teacher, and Parent views.
________________________________________
Enterprise UI Design Specification
Based on your schema's architecture, here is the granular breakdown of what the UI should actually contain to be "World Class":
1. The "Entitlement-Aware" Sidebar
Instead of a hardcoded menu, the sidebar should query the ModuleEntitlement table. If a school hasn't paid for the "Analytics" module, the enabled boolean will be false, and the menu item will be hidden or "locked" with an upgrade CTA.
2. The Dynamic Table Layouts
Because you are using Json for the payload, the tables cannot be static.
Page	Primary Columns (Schema-based)	Payload Columns (Suggested)	Action Buttons
Teachers	id, updatedAt 	Name, Subject, Experience, Bio	Edit, Deactivate
Students	tenantId, createdAt 	Roll No, Class, Section, Blood Group	Edit, View Profile
Exams	configKey 	Term, Max Marks, Date, Passing %	Edit, Schedule
3. The Configuration Management Page
A "Master Settings" page is required to manage the Configuration and ConfigMaster models. This is where the Admin sets the tenantCode and updates the school's global payload (Logo, Address, Contact Info).
+1
4. Event Monitoring
In the footer or a "System Health" tab, use the ProcessedEvent table to show a live log.
•	Event: eventType (e.g., "STUDENT_ADMISSION").
•	ID: eventId (reference to the transaction).
•	Timestamp: processedAt.
--------------
See that all requirements of enterprise grade platform quality compliance are met... some of the requirements (may not these only) are listed below:

	 Multi-tenant safe
	Transaction protected
	Concurrency safe
	Idempotent
	Auditable
	Cache scoped correctly
	Enterprise-safe pattern
	Transaction handling
	tenant validation
	idempotency check
	concurrency control
	audit log
	retry handling


