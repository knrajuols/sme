# Web Admin UI

## Run
- `npm run dev -w @sme/web-admin`

## Port
- `http://localhost:3001`

## Login prerequisites
- Use platform admin / school admin / teacher users already available in SME IAM.
- Current auth endpoint issues token by email; password field is accepted in UI but ignored by backend for now.
